# ──────────────────────────────────────────────────────────────────────────
#  requirements.txt
#  TC-83 | Successful Login with Valid Username and Password
#  SauceDemo Pytest Automation Suite — SOLID + POM Architecture
#
#  Install: pip install -r requirements.txt
#  Python : >= 3.9 recommended
# ──────────────────────────────────────────────────────────────────────────

# ── Selenium WebDriver ────────────────────────────────────────────────── #
selenium>=4.18.1                 # Core browser automation

# ── WebDriver Manager ────────────────────────────────────────────────── #
webdriver-manager>=4.0.1         # Auto-downloads ChromeDriver / GeckoDriver

# ── Pytest Core ──────────────────────────────────────────────────────── #
pytest>=7.4.0                    # Test runner
pytest-html>=4.1.1               # HTML report (--html + --self-contained-html)

# ── Pytest Plugins (optional but recommended) ────────────────────────── #
pytest-xdist>=3.5.0              # Parallel execution: pytest -n auto
pytest-rerunfailures>=13.0       # Auto-retry flaky tests: --reruns 2
